services:
  postgres:
    image: postgres:16
    container_name: factory-database
    environment:
      POSTGRES_DB: factory_db
      POSTGRES_USER: factory
      POSTGRES_PASSWORD: factorymanager
    ports:
      - "5400:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: